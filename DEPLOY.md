# DEPLOY TO VERCEL - EXACT STEPS

## Settings:

- **Root Directory**: `frontend`
- **Framework**: `Vite`
- **Build Command**: `npm install && npm run build`
- **Output Directory**: `dist`
- **Environment Variable**: `VITE_API_URL=/api`

## That's it. Deploy.

This is the ORIGINAL working version from ombudsman_full_deploy.zip with minimal vercel.json.
